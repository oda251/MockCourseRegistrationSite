export type User = {
  id: number;
  email: string;
  name: string;
  password: string;
  credits: number;
};

export type UserInfo = {
  id: number;
  name: string;
  email: string;
  credits: number;
};
